name: jenkins31
on:
  workflow_dispatch:
env:
#   # This item has no matching transformer
#   JOB_NAME: ${"${{ github.workflow }}".replace("/", "-")}
  GIT_SSL_NO_VERIFY: 'true'
  URL_TO_TEST: https://google.com
  PR_GITHUB_USERNAME: labs-robot
#   # This item has no matching transformer
#   OCP_TOKEN:
#   # This item has no matching transformer
#   PR_GITHUB_TOKEN:
jobs:
  Prepare_Environment:
    name: Prepare Environment
    runs-on:
      - self-hosted
      - master
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             timeout(time: 1, unit: 'HOURS') {
#                                     env.PR_ID = input(
#                                         message: 'Which PR # do you want to test?',
#                                         parameters: [string(name: 'pr', description: 'PR Number')]
#                                     )
#                                 }
#
#                                 // Validate input
#                                 if (!env.PR_ID?.trim()) {
#                                     error("PR_ID cannot be empty")
#                                 }
#
#                                 // Set derived environment variables
#                                 env.PR_CI_CD_PROJECT_NAME = "labs-ci-cd-pr-${{ env.PR_ID }}"
#                                 env.PR_DEV_PROJECT_NAME = "labs-dev-pr-${{ env.PR_ID }}"
#                                 env.PR_TEST_PROJECT_NAME = "labs-test-pr-${{ env.PR_ID }}"
#                                 env.USER_PASS = "${{ env.PR_GITHUB_USERNAME }}:${{ env.PR_GITHUB_TOKEN }}"
#                                 env.PR_BRANCH = "pull/${{ env.PR_ID }}/head"
#                                 env.PR_URI = "https://api.github.com/repos/rht-labs/labs-ci-cd/pulls/${{ env.PR_ID }}"
  Clear_Existing_Projects:
    name: Clear Existing Projects
    runs-on:
      - self-hosted
      - master
    needs: Prepare_Environment
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             openshift.withCluster(env.OCP_API_SERVER, env.OCP_TOKEN) {
#                                     [env.PR_CI_CD_PROJECT_NAME, env.PR_DEV_PROJECT_NAME, env.PR_TEST_PROJECT_NAME].each { project ->
#                                         if (openshift.projectExists(project)) {
#                                             openshift.deleteProject(project)
#                                         }
#                                     }
#                                 }
  Ansible_Operations_Merge_PR:
    name: Ansible Operations-Merge PR
    runs-on: ubuntu-latest
    needs: Clear_Existing_Projects
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: Jenkins
#       - key: description
#         value:
#           isLiteral: true
#           value: ALL CI jobs are running...
#       - key: status
#         value:
#           isLiteral: true
#           value: PENDING
    - name: sh
      shell: bash
      run: |-
        git config --global user.email "labs.robot@gmail.com"
        git config --global user.name "Labs Robot"
        git checkout master
        git fetch origin ${{ env.PR_BRANCH }}:pr
        git merge pr --ff
  Ansible_Operations_Apply_Inventory:
    name: Ansible Operations-Apply Inventory
    runs-on: ubuntu-latest
    needs: Ansible_Operations_Merge_PR
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: sh
      shell: bash
      run: ansible-galaxy install -r requirements.yml --roles-path=roles
    - name: sh
      shell: bash
      run: '"ansible-playbook site.yml -e ci_cd_namespace=${{ env.PR_CI_CD_PROJECT_NAME }} -e dev_namespace=${{ env.PR_DEV_PROJECT_NAME }} -e test_namespace=${{ env.PR_TEST_PROJECT_NAME }} -e role=admin"'
    - name: snapshot post build job status
      run: |-
        echo "success=${{ job.status == 'success' }}" >> $GITHUB_OUTPUT
        echo "failure=${{ job.status == 'failure' }}" >> $GITHUB_OUTPUT
      id: post_build
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: Apply Inventory
#       - key: status
#         value:
#           isLiteral: true
#           value: FAILURE
#       if: steps.post_build.outputs.failure == 'true'
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: Apply Inventory
#       - key: status
#         value:
#           isLiteral: true
#           value: SUCCESS
#       if: steps.post_build.outputs.success == 'true'
  Verify_CI_Builds:
    name: Verify CI Builds
    runs-on:
      - self-hosted
      - master
    needs: Ansible_Operations_Apply_Inventory
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Builds
#       - key: description
#         value:
#           isLiteral: true
#           value: job is running...
#       - key: status
#         value:
#           isLiteral: true
#           value: PENDING
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             openshift.withCluster(env.OCP_API_SERVER, env.OCP_TOKEN) {
#                                     openshift.withProject(env.PR_CI_CD_PROJECT_NAME) {
#                                         timeout(time: 5, unit: 'MINUTES') {
#                                             def builds = openshift.selector('bc').objects
#                                             builds.each { bc ->
#                                                 if (bc.spec.strategy.type != "Pipeline") {
#                                                     def latestBuild = openshift.selector("bc/${bc.metadata.name}").related('builds').max()
#                                                     latestBuild.untilEach { return it.object().status.phase == "Complete" }
#                                                 }
#                                             }
#                                         }
#                                     }
#                                 }
    - name: snapshot post build job status
      run: |-
        echo "success=${{ job.status == 'success' }}" >> $GITHUB_OUTPUT
        echo "failure=${{ job.status == 'failure' }}" >> $GITHUB_OUTPUT
      id: post_build
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Builds
#       - key: status
#         value:
#           isLiteral: true
#           value: FAILURE
#       if: steps.post_build.outputs.failure == 'true'
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Builds
#       - key: status
#         value:
#           isLiteral: true
#           value: SUCCESS
#       if: steps.post_build.outputs.success == 'true'
  Verify_CI_Deploys:
    name: Verify CI Deploys
    runs-on:
      - self-hosted
      - master
    needs: Verify_CI_Builds
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Deploys
#       - key: description
#         value:
#           isLiteral: true
#           value: job is running...
#       - key: status
#         value:
#           isLiteral: true
#           value: PENDING
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             openshift.withCluster(env.OCP_API_SERVER, env.OCP_TOKEN) {
#                                     openshift.withProject(env.PR_CI_CD_PROJECT_NAME) {
#                                         timeout(time: 10, unit: 'MINUTES') {
#                                             openshift.selector('dc').each { dc ->
#                                                 dc.rollout().status()
#                                             }
#                                         }
#                                     }
#                                 }
    - name: snapshot post build job status
      run: |-
        echo "success=${{ job.status == 'success' }}" >> $GITHUB_OUTPUT
        echo "failure=${{ job.status == 'failure' }}" >> $GITHUB_OUTPUT
      id: post_build
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Deploys
#       - key: status
#         value:
#           isLiteral: true
#           value: FAILURE
#       if: steps.post_build.outputs.failure == 'true'
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: CI Deploys
#       - key: status
#         value:
#           isLiteral: true
#           value: SUCCESS
#       if: steps.post_build.outputs.success == 'true'
  Post_Cleanup:
    if: github.ref == 'refs/heads/main'
    name: Post-Cleanup
    runs-on:
      - self-hosted
      - master
    needs: Verify_CI_Deploys
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # 'script' was not transformed because there is no suitable equivalent in GitHub Actions
#     - name: script
#       arguments:
#       - key: scriptBlock
#         value:
#           isLiteral: true
#           value: |-
#             openshift.withCluster(env.OCP_API_SERVER, env.OCP_TOKEN) {
#                                     [env.PR_CI_CD_PROJECT_NAME, env.PR_DEV_PROJECT_NAME, env.PR_TEST_PROJECT_NAME].each { project ->
#                                         if (openshift.projectExists(project)) {
#                                             openshift.deleteProject(project)
#                                         }
#                                     }
#                                 }
  Post-Build:
    if: always()
    name: Post Build
    runs-on: ubuntu-latest
    needs:
    - Prepare_Environment
    - Clear_Existing_Projects
    - Ansible_Operations_Merge_PR
    - Ansible_Operations_Apply_Inventory
    - Verify_CI_Builds
    - Verify_CI_Deploys
    - Post_Cleanup
    steps:
    - name: snapshot post build workflow status
      run: |-
        echo "success=${{ contains(needs.*.result,'success') && !contains(needs.*.result,'cancelled') && !contains(needs.*.result,'failure') }}" >> $GITHUB_OUTPUT
        echo "failure=${{ contains(needs.*.result,'failure') && !contains(needs.*.result,'cancelled') }}" >> $GITHUB_OUTPUT
      id: post_build
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: Jenkins
#       - key: status
#         value:
#           isLiteral: true
#           value: FAILURE
#       if: steps.post_build.outputs.failure == 'true'
#     # This item has no matching transformer
#     - githubNotify:
#       - key: context
#         value:
#           isLiteral: true
#           value: Jenkins
#       - key: status
#         value:
#           isLiteral: true
#           value: SUCCESS
#       if: steps.post_build.outputs.success == 'true'
    - name: clean workspace
      shell: bash
      run: rm -rf ${{ github.workspace }}/*
      if: always()
